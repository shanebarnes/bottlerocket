environment:
  matrix:
    - Compiler: mingw
    - Config: Release

cache:
  - C:\cygwin64

install:
  - set PATH=C:\cygwin64\bin;%PATH%
  - set CC=gcc

build_script:
  - cmake -H. -Bbuild
  - cd build
  - dir
